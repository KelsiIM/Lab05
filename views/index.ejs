<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Toys</title>
    <link rel="stylesheet" href="mystyles.css">
</head>
<body>

    <%- include('header') %>

    <main>
        <div class="grid-row">
            <% mydata.forEach((toy) => { %>
             <div class="grid-item">
                <%= toy.title.slice(0, 30) %> <%# slice method removes characters - in this example its after 30 chars %>
                <div class="b"><a href="<%= toy.url %>" target="_blank">
                    <button>Buy</button>
                </a></div>

             </div>
            <% }) %>
        </div>    
    </main>

    <%- include('footer') %>
    
</body>
</html>